- For each abitlities add the possibility to choose a condition : a checkbox and a dropdown list.
	conditions :	(buff|debuff) (active|inactive) on (target|player)
					if [abilities] is on cooldown
					if target is below x% hp
- Add option for trinket : 2 dropdown list with effect (like + power) and a box to enter the value
- Add option for set bonus
- Change force to dynamic ressource